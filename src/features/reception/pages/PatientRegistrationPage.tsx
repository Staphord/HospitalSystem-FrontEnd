import { ComingSoonPage } from '@/components/ui/ComingSoonPage'

export function PatientRegistrationPage() {
  return (
    <ComingSoonPage
      title="Patient registration"
      description="Waiting on POST /api/v1/patients via api-gateway → reception-service."
    />
  )
}
