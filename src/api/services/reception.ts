// Proxied via api-gateway → reception-service
// Planned endpoints: /patients, /visits, /visits/{id}/insurance, /queue

export const receptionService = {
  // listPatients: () => apiClient.get('/patients')
  // registerPatient: (data) => apiClient.post('/patients', data)
  // createVisit: (data) => apiClient.post('/visits', data)
  // getQueue: () => apiClient.get('/queue')
}
